Compiling from source
=====================

Pelican is compiled by the cmake build system using the following steps.

    mkdir build
    cd build
    cmake ../path/to/top/level/CMakeLists.txt
    make [build options]

For build options see:
    readme.txt